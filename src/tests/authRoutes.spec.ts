/*
 * This file is commented out since it breaks the code coverage generator
 * but serves as an example of how we can automatically test the routes
 * of the system.
 */

// import * as supertest from 'supertest';
// import server from '../index';

// describe('Auth POST /login', function() {
//     it("should return status code 400 if no body", function(done) {
//         supertest(server)
//             .post("/api/auth/login")
//             .expect(400)
//             .end(function(err, res) {
//                 if(err) done(err);
//                 done();
//             });
//     });
// });
